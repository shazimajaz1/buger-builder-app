import React from 'react'
import classes from './Spinner.css'

/*
    This is a spinner element.
 */
const spinner = () => (
    <div className={classes.Loader}>Loading...</div>
);

export default spinner;